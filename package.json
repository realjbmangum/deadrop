{
  "name": "deadrop",
  "version": "1.0.0",
  "description": "Zero-knowledge self-destructing secret sharing on Cloudflare",
  "private": false,
  "scripts": {
    "setup": "node scripts/setup.cjs",
    "dev": "astro dev",
    "build": "astro build",
    "preview": "wrangler pages dev ./dist",
    "deploy": "astro build && wrangler pages deploy ./dist",
    "types": "astro sync"
  },
  "dependencies": {
    "astro": "^4.0.0",
    "@astrojs/cloudflare": "^10.0.0",
    "@astrojs/tailwind": "^5.0.0",
    "tailwindcss": "^3.4.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.0.0",
    "typescript": "^5.0.0",
    "wrangler": "^3.0.0"
  }
}
